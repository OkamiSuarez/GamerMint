
const Events = () => {

    // const handleClick = (e) =>{
    //     console.log(e.screenX, e.screenY)
    // }
    // window.addEventListener("click", handleClick)

  return (
    <div>Events</div>
  )
}

export default Events